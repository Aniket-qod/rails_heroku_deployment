<%= form_with(model: video_theme) do |form| %>
  <% if video_theme.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(video_theme.errors.count, "error") %> prohibited this video_theme from being saved:</h2>

      <ul>
        <% video_theme.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :video, style: "display: block" %>
    <%= form.text_field :video %>
  </div>

  <div>
    <%= form.label :status, style: "display: block" %>
    <%= form.text_field :status %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
